spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dschadow/CloudSecurity
          search-paths: config-repo

management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  port: 8888

logging:
  level:
    root: info