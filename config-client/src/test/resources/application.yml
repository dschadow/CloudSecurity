application:
  name: Config Client
  profile: Test

spring:
  datasource:
    name: config-client-db
    username: config-client-db-user
    password: config-client-db-password
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    root: info
