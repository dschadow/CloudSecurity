spring:
  application:
    name: config-client-vault
  cloud:
    vault:
      host: localhost # localhost is the default
      port: 8200 # 8200 is the default
      scheme: http # only for test environment
      token: 230b71cf-6db7-0573-6902-b538df395d01 # update this with a valid token