application:
  name: Config Client Vault
  profile: Default

management:
  security:
    enabled: false

spring:
  boot:
    admin:
      url: http://localhost:8888
  h2:
    console:
      enabled: true